<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout raiz de la unica Activity de la app (Single Activity pattern).

    Estructura:
      DrawerLayout (raiz)
        CoordinatorLayout (contenido principal)
          AppBarLayout
            MaterialToolbar  <- toolbar configurable por fragment
          NavHostFragment    <- contenedor de todos los fragments
        NavigationView       <- panel lateral (Drawer)

    El Drawer es dinamico: MainActivity muestra u oculta los grupos
    del menu (drawer_menu.xml) segun el rol leido del DataStore.
    En la zona publica (P01 Terminal, P02 Login y similares) el
    DrawerLayout se bloquea para que no se pueda abrir con gesto.

    El NavHostFragment referencia @navigation/nav_graph.
    La Toolbar se conecta al NavController en MainActivity para
    mostrar automaticamente el boton Atras o el icono de hamburguesa
    segun el destino actual (AppBarConfiguration).
-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Contenido principal -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize">

                <!--
                    Logo "StaffFlow" centrado en la toolbar.
                    Aparece en todas las pantallas donde la toolbar es visible
                    (pantallas autenticadas + publicas no-kiosk).
                    tvToolbarNombre se muestra solo en pantallas autenticadas.
                -->
                <LinearLayout
                    android:id="@+id/toolbarBrand"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_gravity="center">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Staff"
                        android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
                        android:fontFamily="sans-serif"
                        android:textStyle="bold"
                        android:textColor="?attr/colorPrimary"
                        android:includeFontPadding="false" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Flow"
                        android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
                        android:fontFamily="sans-serif-thin"
                        android:textColor="?attr/colorPrimary"
                        android:includeFontPadding="false" />

                    <!-- Nombre del usuario — visible solo en pantallas autenticadas -->
                    <TextView
                        android:id="@+id/tvToolbarNombre"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
                        android:textColor="?attr/colorOnSurfaceVariant"
                        android:layout_marginStart="8dp"
                        android:visibility="gone"
                        android:includeFontPadding="false" />

                </LinearLayout>

                <!-- Reloj del sistema — visible solo en pantallas autenticadas -->
                <TextView
                    android:id="@+id/tvReloj"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="end"
                    android:layout_marginEnd="16dp"
                    android:textAppearance="@style/TextAppearance.Material3.BodySmall"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:visibility="gone" />

            </com.google.android.material.appbar.MaterialToolbar>

        </com.google.android.material.appbar.AppBarLayout>

        <!-- Contenedor de fragments. startDestination definido en nav_graph.xml -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/navHostFragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_graph" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Panel lateral (Drawer) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/nav_header_main"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
