<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout del dialogo de cambio de contrasena (E14 PATCH /usuarios/{id}/password).

    Usado en FormUsuarioFragment (P29) modo edicion cuando el ADMIN pulsa
    btnCambiarPassword. La contrasena se muestra en claro con toggle de
    visibilidad (ojo) para que el admin pueda leerla antes de confirmar
    (caso de uso helpdesk: el admin comunica la contrasena al empleado).

    El dialogo NO se cierra si la contrasena tiene menos de 8 caracteres:
    se muestra error inline en tilNuevaPassword (gestionado en Fragment).
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="16dp">

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilNuevaPassword"
        style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/form_usuario_dialogo_password_hint"
        app:endIconMode="password_toggle">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etNuevaPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textVisiblePassword"
            android:autofillHints="password"
            android:singleLine="true" />

    </com.google.android.material.textfield.TextInputLayout>

</LinearLayout>
