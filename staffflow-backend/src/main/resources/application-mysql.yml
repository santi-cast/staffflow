spring:
  datasource:
    url: jdbc:mysql://localhost:3306/staffflow?useSSL=false&serverTimezone=Europe/Madrid&allowPublicKeyRetrieval=true&sslMode=DISABLED
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Europe/Madrid
  jackson:
    serialization:
      write-dates-as-timestamps: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

staffflow:
  mail:
    from: ${MAIL_USERNAME}
    reset-url: staffflow://reset-password?token=
  jwt:
    # JWT_SECRET es OBLIGATORIA en este perfil. El arranque falla si no esta definida (intencional).
    secret: ${JWT_SECRET:?La variable JWT_SECRET es obligatoria para el perfil mysql}
    # Validez del token en milisegundos. 43200000 = 12 horas.
    # 12h cubre una jornada laboral con descanso de comida.
    # Refresh token queda como mejora para v2.0.
    expiration-ms: 43200000
