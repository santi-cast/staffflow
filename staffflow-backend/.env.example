# StaffFlow backend — variables de entorno

# Secreto para firmar los tokens JWT (HS384 con jjwt).
# OBLIGATORIO en produccion (perfil mysql). Opcional en dev (hay fallback en application-dev.yml, pero rotalo si lo expones).
# Generar uno nuevo con: openssl rand -base64 48
JWT_SECRET=
